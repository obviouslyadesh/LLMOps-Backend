# Extends prod dependencies — used locally and in CI only.
# This file is NEVER copied into the Docker image.
-r requirements.txt

# Testing
pytest
pytest-asyncio
httpx                   # needed by FastAPI TestClient

# Linting & formatting
ruff

# Evaluation
ragas
datasets
pandas
pyarrow>=16.0.0         # pin to modern version — older versions try to compile from source on Python 3.12
scikit-learn
scipy