# Extends prod dependencies — used locally and in CI only.
# This file is NEVER copied into the Docker image.
-r requirements.txt

# Testing
pytest
pytest-asyncio
httpx                   # needed by FastAPI TestClient

# Linting & formatting
ruff

# Evaluation
ragas
datasets
pandas
pyarrow==15.0.2         # 16+ removes PyExtensionType which datasets still uses
scikit-learn
scipy